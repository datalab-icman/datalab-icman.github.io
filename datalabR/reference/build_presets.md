# Quick build presets

Preset configurations for common build scenarios

## Usage

``` r
build_quick(pkg_path = ".")

build_full(pkg_path = ".")

build_cran(pkg_path = ".")

build_website(pkg_path = ".", site_destination = NULL)
```
