# Displays a legend of status and progress symbols

Prints a legend of common symbols used in R package console messages,
using their Unicode escape codes.

## Usage

``` r
show_symbol_legend()
```

## Value

Prints the messages to the console.

## Examples

``` r
if (FALSE) { # \dontrun{
show_symbol_legend()
} # }
```
